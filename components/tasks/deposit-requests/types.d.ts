export type DepositRequestModalProps = {
  requestId: string;
  propertyName: string;
  state: string;
  unitDetails: string;
  branch: string;
  amount: string;
};

export type DetailsCheckProps = {
  label: string;
};
