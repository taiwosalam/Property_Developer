import {CSSProperties} from 'react'
export interface SearchInputProps {
  className?: string;
  placeholder?: string;
  textInputClassName?: string;
  searchIconColor?: CSSProperties['color'];
  // onChange?: (data: string) => void;
}
