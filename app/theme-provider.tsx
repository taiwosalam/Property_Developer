"use client";

// Imports
import useThemeColors from "@/hooks/useTheme";

const ThemeProvider = () => {
  useThemeColors(); // Update theme color around the app

  return null;
};

export default ThemeProvider;
